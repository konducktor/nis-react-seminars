{
  "name": "ts-basics-seminar",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "warmup": "tsc --noEmit -p tsconfig.warmup.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^5.0.1"
  }
}
